apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dts-cdc-spool
  namespace: dts
spec:
  # Use an RWX-capable StorageClass such as NFS or CephFS. The offline
  # installer can set it with DTS_RWX_STORAGE_CLASS.
  accessModes: [ReadWriteMany]
  resources:
    requests: {storage: 200Gi}
