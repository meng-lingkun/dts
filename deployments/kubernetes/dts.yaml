apiVersion: v1
kind: Namespace
metadata:
  name: dts
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dts-config
  namespace: dts
data:
  DTS_REPOSITORY: postgres
  DTS_PRODUCTION: "true"
  DTS_METADATA_HOST: postgres.dts.svc
  DTS_METADATA_PORT: "5432"
  DTS_METADATA_USER: dts
  DTS_METADATA_DATABASE: dts
  DTS_AUTH_REQUIRED: "true"
  # The offline installer overrides this value when DTS_CORS_ORIGIN is set.
  DTS_CORS_ORIGIN: http://127.0.0.1:8088
  DTS_DB_PRESSURE_SAMPLE_SECONDS: "5"
  DTS_WORKER_MAX_CPU_PCT: "95"
  DTS_WORKER_MAX_MEMORY_PCT: "95"
  DTS_CDC_SPOOL_STORAGE: file
  DTS_CDC_SPOOL_DIR: /var/lib/dts/cdc-spool
  DTS_CDC_SPOOL_DISK_WARN_PCT: "80"
  DTS_CDC_SPOOL_DISK_CRITICAL_PCT: "90"
  DTS_VALIDATION_STABLE_WINDOW_SECONDS: "2"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: dts
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 0, maxSurge: 1}
  selector:
    matchLabels: {app: dts-server}
  template:
    metadata:
      labels: {app: dts-server}
    spec:
      terminationGracePeriodSeconds: 30
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels: {app: dts-server}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels: {app: dts-server}
      containers:
      - name: server
        image: dts/server:0.15.0-rc49
        imagePullPolicy: Never
        envFrom: [{configMapRef: {name: dts-config}}]
        env:
        - name: DTS_METADATA_PASSWORD
          valueFrom: {secretKeyRef: {name: dts-secrets, key: metadata-password}}
        - name: DTS_MASTER_KEY
          valueFrom: {secretKeyRef: {name: dts-secrets, key: master-key}}
        - name: DTS_WORKER_TOKEN
          valueFrom: {secretKeyRef: {name: dts-secrets, key: worker-token}}
        - name: DTS_AUTH_SECRET
          valueFrom: {secretKeyRef: {name: dts-secrets, key: auth-secret}}
        - name: DTS_BOOTSTRAP_ADMIN_PASSWORD
          valueFrom: {secretKeyRef: {name: dts-secrets, key: bootstrap-admin-password}}
        ports: [{containerPort: 8080, name: http}]
        readinessProbe: {httpGet: {path: /readyz, port: http}, initialDelaySeconds: 3, periodSeconds: 5}
        livenessProbe: {httpGet: {path: /healthz, port: http}, initialDelaySeconds: 15, periodSeconds: 10}
        resources:
          requests: {cpu: "250m", memory: "256Mi"}
          limits: {cpu: "2", memory: "2Gi"}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities: {drop: ["ALL"]}
        volumeMounts:
        - {name: cdc-spool, mountPath: /var/lib/dts/cdc-spool}
        - {name: tmp, mountPath: /tmp}
      volumes:
      - name: cdc-spool
        persistentVolumeClaim: {claimName: dts-cdc-spool}
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: dts
spec:
  selector: {app: dts-server}
  ports: [{port: 8080, targetPort: http}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: server
  namespace: dts
spec:
  minAvailable: 1
  selector:
    matchLabels: {app: dts-server}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: dts
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 1, maxSurge: 1}
  selector:
    matchLabels: {app: dts-worker}
  template:
    metadata:
      labels: {app: dts-worker}
    spec:
      terminationGracePeriodSeconds: 45
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels: {app: dts-worker}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels: {app: dts-worker}
      containers:
      - name: worker
        image: dts/server:0.15.0-rc49
        imagePullPolicy: Never
        command: ["/usr/local/bin/dts-worker"]
        env:
        - {name: DTS_SERVER, value: "http://server.dts.svc:8080"}
        - {name: DTS_WORKER_CONCURRENCY, value: "4"}
        - {name: DTS_WORKER_SHUTDOWN_GRACE_SECONDS, value: "30"}
        - {name: DTS_WORKER_LABELS, value: "network=kubernetes"}
        - name: DTS_WORKER_TOKEN
          valueFrom: {secretKeyRef: {name: dts-secrets, key: worker-token}}
        resources:
          requests: {cpu: "1", memory: "1Gi"}
          limits: {cpu: "4", memory: "4Gi"}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities: {drop: ["ALL"]}
        volumeMounts:
        - {name: tmp, mountPath: /tmp}
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: worker
  namespace: dts
spec:
  minAvailable: 2
  selector:
    matchLabels: {app: dts-worker}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: worker
  namespace: dts
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: worker}
  minReplicas: 3
  maxReplicas: 50
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
  metrics:
  - type: Resource
    resource:
      name: cpu
      target: {type: Utilization, averageUtilization: 70}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: dts
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 0, maxSurge: 1}
  selector:
    matchLabels: {app: dts-web}
  template:
    metadata:
      labels: {app: dts-web}
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels: {app: dts-web}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels: {app: dts-web}
      containers:
      - name: web
        image: dts/web:0.15.0-rc49
        imagePullPolicy: Never
        ports: [{containerPort: 8080, name: http}]
        readinessProbe: {httpGet: {path: /, port: http}, initialDelaySeconds: 3, periodSeconds: 10}
        resources:
          requests: {cpu: "50m", memory: "64Mi"}
          limits: {cpu: "500m", memory: "256Mi"}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities: {drop: ["ALL"]}
        volumeMounts:
        - {name: tmp, mountPath: /tmp}
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: dts
spec:
  selector: {app: dts-web}
  ports: [{port: 80, targetPort: http}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web
  namespace: dts
spec:
  minAvailable: 1
  selector:
    matchLabels: {app: dts-web}
