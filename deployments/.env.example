# Kubernetes configuration reference. Internal secrets must be independent random values.
# The offline Kubernetes installer generates these internal secrets on first install.
# Do not source an unreviewed environment file. See deployments/offline/README.md.

DTS_METADATA_PASSWORD=
DTS_MASTER_KEY=
DTS_WORKER_TOKEN=
DTS_AUTH_SECRET=
DTS_BOOTSTRAP_ADMIN_USER=admin
DTS_BOOTSTRAP_ADMIN_PASSWORD=Cljslrl0620!
DTS_CORS_ORIGIN=http://127.0.0.1:8088
DTS_WORKER_CONCURRENCY=4
DTS_WORKER_SHUTDOWN_GRACE_SECONDS=30
